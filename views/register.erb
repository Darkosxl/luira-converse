<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account – Luira | AI-Powered Conversations</title>
    <meta name="robots" content="noindex, nofollow">
    <meta name="description" content="Create your Luira account – join the AI-powered conversation platform. Get started for free.">
    <link rel="canonical" href="https://luira.amoredit.com/register">

    <!-- Open Graph / Social -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://luira.amoredit.com/register">
    <meta property="og:title" content="Create Account – Luira">
    <meta property="og:description" content="Create your Luira account – join the AI-powered conversation platform.">
    <meta property="og:site_name" content="Luira">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="Create Account – Luira">
    <meta name="twitter:description" content="Create your Luira account – join the AI-powered conversation platform.">

    <!-- JSON-LD Schema -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Luira",
      "url": "https://luira.amoredit.com",
      "applicationCategory": "BusinessApplication",
      "operatingSystem": "Web",
      "provider": {
        "@type": "Organization",
        "name": "Luira",
        "url": "https://luira.amoredit.com"
      }
    }
    </script>

    <link rel="stylesheet" href="/output.css">
    <style>
        /* Brand colors */
        :root {
            --brand-navy:   #050a30;
            --brand-purple: #7910ff;
            --brand-cyan:   #00d2ff;
            --brand-dark:   #0f1547;
        }
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html { font-family: 'Inter', system-ui, sans-serif; font-size: 16px; }
        body {
            background-color: var(--brand-navy);
            color: white;
            min-height: 100svh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            position: relative;
            overflow: hidden;
        }
        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background-image:
                linear-gradient(rgba(121, 16, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(121, 16, 255, 0.03) 1px, transparent 1px);
            background-size: 50px 50px;
            pointer-events: none;
        }
        /* Orbs — blur reduced 80px → 40px (M-1) */
        .orb { position: absolute; border-radius: 50%; filter: blur(40px); opacity: 0.4; will-change: transform; }

        /* Code digit boxes */
        .digit-box {
            width: 48px; height: 56px;
            background: rgba(5,10,48,0.5);
            border: 1.5px solid rgba(121,16,255,0.3);
            border-radius: 12px;
            color: white;
            font-size: 22px;
            font-weight: 700;
            text-align: center;
            outline: none;
            transition: border-color 0.2s, box-shadow 0.2s, transform 0.15s;
            caret-color: #00d2ff;
        }
        .digit-box:focus {
            border-color: #7910ff;
            box-shadow: 0 0 18px rgba(121,16,255,0.3);
            transform: scale(1.05);
        }
        .digit-box.filled { border-color: #00d2ff; }
        .digit-box.error  { border-color: #ef4444 !important; box-shadow: 0 0 12px rgba(239,68,68,0.3) !important; animation: shake 0.4s ease; }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            20%       { transform: translateX(-5px); }
            40%       { transform: translateX(5px); }
            60%       { transform: translateX(-4px); }
            80%       { transform: translateX(4px); }
        }

        /* Send code button states */
        .send-btn-idle    { background: rgba(121,16,255,0.15); border: 1.5px solid rgba(121,16,255,0.4); color: #a78bfa; }
        .send-btn-loading { background: rgba(121,16,255,0.08); border: 1.5px solid rgba(121,16,255,0.2); color: #6b7280; cursor: not-allowed; }
        .send-btn-sent    { background: rgba(0,210,255,0.12); border: 1.5px solid rgba(0,210,255,0.3); color: #67e8f9; }

        /* Countdown */
        #countdown { font-variant-numeric: tabular-nums; }
    </style>
</head>
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50%       { transform: translateY(-10px); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to   { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to   { opacity: 1; }
        }
        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-10px) scale(0.97); }
            to   { opacity: 1; transform: translateY(0) scale(1); }
        }
        @keyframes glowPulse {
            from { opacity: 0.4; }
            to   { opacity: 1; }
        }
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            20%       { transform: translateX(-5px); }
            40%       { transform: translateX(5px); }
            60%       { transform: translateX(-4px); }
            80%       { transform: translateX(4px); }
        }
        .anim-float { animation: float 8s ease-in-out infinite; }
        .anim-fade-in-up { animation: fadeInUp 0.9s ease-out both; }
        .anim-fade-in    { animation: fadeIn 0.4s ease-out both; }
        .anim-slide-down { animation: slideDown 0.35s cubic-bezier(0.34,1.56,0.64,1) both; }

        /* Card — backdrop-blur reduced from 24px → 8px (M-2) */
        .card {
            background: linear-gradient(135deg, rgba(15,21,71,0.92), rgba(31,41,55,0.92));
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            padding: 3rem;
            border-radius: 1.5rem;
            border: 1px solid rgba(121, 16, 255, 0.2);
            max-width: 28rem;
            width: 100%;
            position: relative;
            z-index: 10;
            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
        }
        /* Pulse glow via opacity on ::after — avoids main-thread box-shadow repaints (H-6) */
        .card::after {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: 1.5rem;
            box-shadow: 0 0 40px rgba(121, 16, 255, 0.5);
            pointer-events: none;
            animation: glowPulse 2s ease-in-out infinite alternate;
        }
        .logo-icon {
            width: 4rem; height: 4rem;
            border-radius: 1rem;
            background: linear-gradient(135deg, var(--brand-cyan), var(--brand-purple));
            display: flex; align-items: center; justify-content: center;
            margin: 0 auto 1.5rem;
        }
        /* Heading — solid fallback so Chrome picks as LCP candidate (L-6) */
        .heading-gradient {
            color: white;
            background: linear-gradient(-45deg, #ffffff, #00d2ff, #7910ff, #00d2ff, #ffffff);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-size: 1.875rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 0.5rem;
        }
        /* Inputs — 16px minimum (M-6) */
        .field-label {
            display: block;
            font-size: 1rem;
            font-weight: 500;
            color: #d1d5db;
            margin-bottom: 0.5rem;
        }
        .field-input {
            width: 100%;
            padding: 1rem 1rem 1rem 3rem;
            background: rgba(5, 10, 48, 0.5);
            border: 1px solid rgba(121, 16, 255, 0.3);
            border-radius: 0.75rem;
            color: white;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        .field-input::placeholder { color: #6b7280; }
        .field-input:focus {
            border-color: var(--brand-purple);
            box-shadow: 0 0 20px rgba(121, 16, 255, 0.25);
        }
        .input-wrapper { position: relative; }
        .input-icon {
            position: absolute; top: 0; left: 0;
            height: 100%; padding-left: 1rem;
            display: flex; align-items: center;
            pointer-events: none;
        }
        .input-icon svg { width: 1.25rem; height: 1.25rem; color: #6b7280; }
        .form-space { margin-bottom: 1.25rem; }
        .error-banner {
            background: rgba(239, 68, 68, 0.15);
            border: 1px solid rgba(239, 68, 68, 0.5);
            color: #fca5a5;
            padding: 0.75rem 1rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        /* Code digit boxes */
        .digit-box {
            width: 48px; height: 56px;
            background: rgba(5,10,48,0.5);
            border: 1.5px solid rgba(121,16,255,0.3);
            border-radius: 12px;
            color: white;
            font-size: 22px;
            font-weight: 700;
            text-align: center;
            outline: none;
            transition: border-color 0.2s, box-shadow 0.2s, transform 0.15s;
            caret-color: #00d2ff;
        }
        .digit-box:focus {
            border-color: #7910ff;
            box-shadow: 0 0 18px rgba(121,16,255,0.3);
            transform: scale(1.05);
        }
        .digit-box.filled { border-color: #00d2ff; }
        .digit-box.error  { border-color: #ef4444 !important; box-shadow: 0 0 12px rgba(239,68,68,0.3) !important; animation: shake 0.4s ease; }

        /* Send code button states */
        .send-btn-idle    { background: rgba(121,16,255,0.15); border: 1.5px solid rgba(121,16,255,0.4); color: #a78bfa; }
        .send-btn-loading { background: rgba(121,16,255,0.08); border: 1.5px solid rgba(121,16,255,0.2); color: #6b7280; cursor: not-allowed; }
        .send-btn-sent    { background: rgba(0,210,255,0.12); border: 1.5px solid rgba(0,210,255,0.3); color: #67e8f9; }

        /* Submit button */
        .btn-submit {
            width: 100%;
            padding: 1rem 1.5rem;
            background: linear-gradient(135deg, var(--brand-cyan), var(--brand-purple));
            color: white;
            border: none;
            border-radius: 0.75rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            margin-top: 0.5rem;
            transition: transform 0.3s, box-shadow 0.3s, opacity 0.2s;
        }
        .btn-submit:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 20px 40px -12px rgba(0, 210, 255, 0.4);
        }
        .btn-submit:focus { outline: none; box-shadow: 0 0 0 4px rgba(0,210,255,0.3); }
        .btn-submit:disabled { opacity: 0.4; cursor: not-allowed; }

        /* Countdown */
        #countdown { font-variant-numeric: tabular-nums; }

        /* Footer */
        .footer-link-row {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(55, 65, 81, 0.5);
            text-align: center;
        }
        .footer-text { color: #9ca3af; font-size: 1rem; }
        /* Min 48px tap target for register link (M-7) */
        .footer-link {
            display: inline-block;
            padding: 0.75rem 0.5rem;
            min-height: 48px;
            line-height: 48px;
            vertical-align: middle;
            color: var(--brand-purple);
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }
        .footer-link:hover { color: white; }

        /* prefers-reduced-motion (L-5) */
        @media (prefers-reduced-motion: reduce) {
            .anim-float, .anim-fade-in-up, .anim-fade-in, .anim-slide-down, .card::after { animation: none; }
            .orb { opacity: 0.2; }
        }
        /* Mobile layout (M-8) */
        @media (max-width: 480px) {
            .card { padding: 2rem 1.5rem; }
            body { padding: 1rem; }
        }
    </style>
</head>
<body>
    <div class="orb anim-float" style="width:24rem;height:24rem;background:var(--brand-cyan);top:-12rem;right:-12rem;animation-delay:0s;"></div>
    <div class="orb anim-float" style="width:20rem;height:20rem;background:var(--brand-purple);bottom:-10rem;left:-10rem;animation-delay:-4s;"></div>

    <div class="card anim-fade-in-up">
        <!-- Logo -->
        <div class="logo-icon anim-float" style="animation-delay:-1s;">
            <svg style="width:2rem;height:2rem;color:white;" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"/>
            </svg>
        </div>

        <div style="text-align:center;margin-bottom:2rem;">
            <h1 class="heading-gradient">Create Account</h1>
            <p style="color:#9ca3af;font-size:1rem;">Verify your email to get started</p>
        </div>

        <!-- Error / success banners -->
        <% if @error %>
            <div id="server-error" class="error-banner anim-fade-in" role="alert">
                <svg style="width:1.25rem;height:1.25rem;flex-shrink:0;" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                <p style="font-size:0.875rem;font-weight:500;"><%= @error %></p>
            </div>
        <% end %>

        <form id="register-form" method="POST" action="/register" onsubmit="return validateBeforeSubmit()" style="margin-top:0;">

            <!-- Email row with Send Code button -->
            <div class="form-space">
                <label for="email" class="field-label">Email Address</label>
                <div style="display:flex;gap:0.5rem;">
                    <div class="input-wrapper" style="flex:1;">
                        <div class="input-icon" aria-hidden="true">
                            <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"/></svg>
                        </div>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            placeholder="you@example.com"
                            required
                            autofocus
                            autocomplete="email"
                            value="<%= @prefill_email || '' %>"
                            class="field-input"
                            style="padding-right:1rem;"
                        >
                    </div>
                    <button type="button" id="send-code-btn"
                            onclick="sendCode()"
                            style="border-radius:0.75rem;font-size:0.875rem;font-weight:600;white-space:nowrap;flex-shrink:0;padding:0 1rem;cursor:pointer;transition:all 0.2s;"
                            class="send-btn-idle">
                        Send Code
                    </button>
                </div>
                <p id="send-status" style="margin-top:0.375rem;font-size:0.75rem;color:#6b7280;display:none;"></p>
            </div>

            <!-- 6-digit code section (hidden until code sent) -->
            <div id="code-section" style="display:none;" class="anim-slide-down form-space">
                <label class="field-label">Verification Code</label>
                <div style="display:flex;gap:0.5rem;justify-content:center;margin-bottom:0.5rem;" id="digit-inputs">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="0" id="d0" autocomplete="one-time-code">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="1" id="d1" autocomplete="off">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="2" id="d2" autocomplete="off">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="3" id="d3" autocomplete="off">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="4" id="d4" autocomplete="off">
                    <input class="digit-box" type="text" inputmode="numeric" maxlength="1" data-index="5" id="d5" autocomplete="off">
                </div>
                <!-- Hidden input that carries the assembled code -->
                <input type="hidden" name="verification_code" id="verification_code">
                <p style="text-align:center;font-size:0.75rem;color:#6b7280;">
                    Didn't get it? <button type="button" id="resend-btn" onclick="sendCode(true)" style="color:var(--brand-cyan);text-decoration:underline;background:none;border:none;cursor:pointer;transition:color 0.2s;" disabled>
                        Resend <span id="countdown"></span>
                    </button>
                </p>
            </div>

            <!-- Password -->
            <div class="form-space">
                <label for="password" class="field-label">Password</label>
                <div class="input-wrapper">
                    <div class="input-icon" aria-hidden="true">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
                    </div>
                    <input type="password" id="password" name="password" placeholder="••••••••" required minlength="8"
                           autocomplete="new-password"
                           class="field-input">
                </div>
                <p style="margin-top:0.5rem;font-size:0.75rem;color:#6b7280;">Must be at least 8 characters</p>
            </div>

            <!-- Confirm Password -->
            <div class="form-space">
                <label for="password_confirm" class="field-label">Confirm Password</label>
                <div class="input-wrapper">
                    <div class="input-icon" aria-hidden="true">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
                    </div>
                    <input type="password" id="password_confirm" name="password_confirm" placeholder="••••••••" required minlength="8"
                           autocomplete="new-password"
                           class="field-input">
                </div>
            </div>

            <!-- Client-side error -->
            <div id="client-error" style="display:none;" class="error-banner anim-fade-in" role="alert">
                <svg style="width:1.25rem;height:1.25rem;flex-shrink:0;" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                <p style="font-size:0.875rem;font-weight:500;" id="client-error-text"></p>
            </div>

            <button type="submit" id="submit-btn" class="btn-submit">
                Create Account
            </button>
        </form>

        <div class="footer-link-row">
            <p class="footer-text">
                Already have an account?
                <a href="/login" class="footer-link">Sign in</a>
            </p>
        </div>
    </div>

<script>
let codeSent = false;
let resendTimer = null;

// ── Digit box behaviour ──────────────────────────────────────────────────────
document.querySelectorAll('.digit-box').forEach((box, i, boxes) => {
    box.addEventListener('keydown', (e) => {
        if (e.key === 'Backspace') {
            if (box.value === '' && i > 0) boxes[i - 1].focus();
            box.classList.remove('filled');
        }
        // Allow only digits
        if (e.key.length === 1 && !/\d/.test(e.key) && !e.ctrlKey && !e.metaKey) e.preventDefault();
    });

    box.addEventListener('input', () => {
        if (box.value.length === 1) {
            box.classList.add('filled');
            box.classList.remove('error');
            if (i < boxes.length - 1) boxes[i + 1].focus();
        }
        assembleCode();
    });

    // Paste handler — spread digits across boxes
    box.addEventListener('paste', (e) => {
        e.preventDefault();
        const digits = e.clipboardData.getData('text').replace(/\D/g, '').slice(0, 6);
        digits.split('').forEach((d, j) => {
            if (boxes[i + j]) {
                boxes[i + j].value = d;
                boxes[i + j].classList.add('filled');
            }
        });
        const next = Math.min(i + digits.length, boxes.length - 1);
        boxes[next].focus();
        assembleCode();
    });
});

function assembleCode() {
    const code = Array.from(document.querySelectorAll('.digit-box')).map(b => b.value).join('');
    document.getElementById('verification_code').value = code;
}

// ── Send / resend code ───────────────────────────────────────────────────────
async function sendCode(isResend = false) {
    const email = document.getElementById('email').value.trim();
    if (!email || !email.includes('@')) {
        showClientError('Please enter a valid email address first.');
        return;
    }

    const btn = document.getElementById('send-code-btn');
    const status = document.getElementById('send-status');

    btn.disabled = true;
    btn.className = 'send-btn-loading';
    btn.textContent = 'Sending…';

    try {
        const res = await fetch('/register/send-code', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: `email=${encodeURIComponent(email)}`
        });
        const data = await res.json();

        if (res.ok) {
            codeSent = true;
            btn.className = 'send-btn-sent';
            btn.textContent = '✓ Sent';

            status.textContent = `Code sent to ${email} — check your inbox (and spam).`;
            status.style.color = '#00d2ff';

            // Show code section
            const section = document.getElementById('code-section');
            section.style.display = 'block';
            setTimeout(() => document.getElementById('d0').focus(), 100);

            // Lock email field
            document.getElementById('email').readOnly = true;

            // Start 60s resend countdown
            startResendTimer();
        } else {
            throw new Error(data.error || 'Failed to send code.');
        }
    } catch (err) {
        btn.disabled = false;
        btn.className = 'send-btn-idle';
        btn.textContent = 'Send Code';
        showClientError(err.message);
    }
}

function startResendTimer() {
    const resendBtn = document.getElementById('resend-btn');
    const countdownEl = document.getElementById('countdown');
    resendBtn.disabled = true;
    let secs = 60;

    if (resendTimer) clearInterval(resendTimer);
    resendTimer = setInterval(() => {
        secs--;
        countdownEl.textContent = `(${secs}s)`;
        if (secs <= 0) {
            clearInterval(resendTimer);
            resendBtn.disabled = false;
            countdownEl.textContent = '';
        }
    }, 1000);
}

// ── Form validation before submit ───────────────────────────────────────────
function validateBeforeSubmit() {
    hideClientError();

    const code = document.getElementById('verification_code').value;
    if (!codeSent || code.length !== 6) {
        showClientError('Please verify your email first — enter the 6-digit code sent to your inbox.');
        // Flash the digit boxes
        document.querySelectorAll('.digit-box').forEach(b => {
            b.classList.add('error');
            setTimeout(() => b.classList.remove('error'), 600);
        });
        // Show the code section if hidden
        document.getElementById('code-section').style.display = 'block';
        return false;
    }

    const pw  = document.getElementById('password').value;
    const pw2 = document.getElementById('password_confirm').value;
    if (pw !== pw2) {
        showClientError('Passwords do not match.');
        return false;
    }

    return true;
}

function showClientError(msg) {
    const el = document.getElementById('client-error');
    document.getElementById('client-error-text').textContent = msg;
    el.style.display = 'flex';
}
function hideClientError() {
    document.getElementById('client-error').style.display = 'none';
}

// If server returned an error, re-show the code section (user may have had a wrong code)
<% if @error && @prefill_email %>
document.addEventListener('DOMContentLoaded', () => {
    codeSent = true;
    document.getElementById('code-section').style.display = 'block';
    document.getElementById('email').readOnly = true;
    document.getElementById('send-code-btn').className = 'send-btn-sent';
    document.getElementById('send-code-btn').textContent = '✓ Sent';
});
<% end %>
</script>
</body>
</html>

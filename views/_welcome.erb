<!-- Welcome Message -->
<div class="text-center mt-12">
  <h1 class="text-2xl font-bold text-gray-900 mb-2">Hello there!</h1>
  <p class="text-gray-600 mb-6">How can I help you today?</p>
  
  <!-- Suggested Prompts -->
  <div class="grid grid-cols-2 gap-2 max-w-xl mx-auto mb-6">
    <button class="p-3 text-left border border-gray-200 rounded-lg hover:bg-gray-50 text-sm text-gray-700"
            onclick="document.querySelector('input[name=message]').value = this.textContent.trim()">
      What are the advantages of using Next.js?
    </button>
    <button class="p-3 text-left border border-gray-200 rounded-lg hover:bg-gray-50 text-sm text-gray-700"
            onclick="document.querySelector('input[name=message]').value = this.textContent.trim()">
      Write code to demonstrate Dijkstra's algorithm
    </button>
    <button class="p-3 text-left border border-gray-200 rounded-lg hover:bg-gray-50 text-sm text-gray-700"
            onclick="document.querySelector('input[name=message]').value = this.textContent.trim()">
      Help me write an essay about Silicon Valley
    </button>
    <button class="p-3 text-left border border-gray-200 rounded-lg hover:bg-gray-50 text-sm text-gray-700"
            onclick="document.querySelector('input[name=message]').value = this.textContent.trim()">
      What is the weather in San Francisco?
    </button>
  </div>
</div>
